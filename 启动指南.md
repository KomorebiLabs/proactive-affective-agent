# 婉情AI (Wanqing-AI) 启动指南

> 本指南将一步步引导你完成整个项目的启动。标有 **[需手动]** 的步骤需要你在本地环境中特别配置，请仔细阅读说明。

---

## 目录

1. [前置要求](#1-前置要求)
2. [第一步：启动 MySQL 数据库](#2-第一步启动-mysql-数据库)
3. [第二步：启动 Redis（可选）](#3-第二步启动-redis可选)
4. [终端 3：启动 Python 感知服务](#4-终端-3启动-python-感知服务)
5. [终端 4：启动 Python Agent](#5-终端-4启动-python-agent)
6. [终端 2：启动 Java Spring Boot 后端](#6-终端-2启动-java-spring-boot-后端)
7. [终端 1：启动 Vue 前端](#7-终端-1启动-vue-前端)
8. [一键启动脚本](#8-一键启动脚本)
9. [验证运行状态](#9-验证运行状态)
10. [常见问题](#10-常见问题)

---

## 1. 前置要求

在开始之前，请确保你的电脑已安装以下软件。

### 1.1 必需软件

| 软件 | 最低版本 | 推荐版本 | 安装说明 |
|------|---------|---------|---------|
| **JDK** | 21 | JDK 21 LTS | [Oracle JDK 下载](https://www.oracle.com/java/technologies/downloads/#java21)（或 [OpenJDK](https://adoptium.net/)） |
| **Python** | 3.10 | 3.11 / 3.12 | [python.org 下载](https://www.python.org/downloads/)（安装时勾选 **Add Python to PATH**） |
| **Node.js** | 18 | 20 LTS | [nodejs.org 下载](https://nodejs.org/) |
| **MySQL** | 8.0 | 8.0+ | [MySQL 社区版下载](https://dev.mysql.com/downloads/mysql/)（Windows 推荐 `.msi` 安装包） |
| **Git** | 任意 | 最新 | [git-scm.com](https://git-scm.com/download/win) |

### 1.2 检查已安装版本

打开 PowerShell（或 CMD），依次执行：

```powershell
# 检查 Java
java -version

# 检查 Python
python --version

# 检查 Node.js 和 npm
node --version
npm --version

# 检查 Git
git --version
```

# 具体如下：
"""
Microsoft Windows [版本 10.0.26200.8037]
(c) Microsoft Corporation。保留所有权利。

C:\Users\YangYiTao>java -version
java version "21.0.8" 2025-07-15 LTS
Java(TM) SE Runtime Environment (build 21.0.8+12-LTS-250)
Java HotSpot(TM) 64-Bit Server VM (build 21.0.8+12-LTS-250, mixed mode, sharing)

C:\Users\YangYiTao>python --version
Python 3.10.6

C:\Users\YangYiTao>node --version
v24.13.0

C:\Users\YangYiTao>npm --version
11.6.2

C:\Users\YangYiTao>git --version
git version 2.50.1.windows.1

C:\Users\YangYiTao>

"""

> **如果显示 "不是内部命令"，说明该软件未安装或环境变量未配置好。请先安装对应软件。**

### 1.3 目录结构确认

克隆或解压项目后，确认目录结构如下：

```
Wanqing/          ← 项目根目录
├── backend/       ← Java + Python 感知服务
├── Agent/         ← Python AI Agent
└── frontend/      ← Vue 前端
```

---

## 1.4 **[需手动] 创建 Python 虚拟环境（本项目有2个！）**

> **重要**：本项目有两个独立的 Python 服务，每个都需要自己的虚拟环境：
> - **perception/venv** — 感知服务（摄像头、音频、MediaPipe）
> - **Agent/venv2.0** — AI Agent（LangGraph、DeepSeek、RAG）
>
> 两者依赖不同，不能混用！

### perception 虚拟环境

```powershell
cd perception
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

### Agent 虚拟环境

```powershell
cd Agent
python -m venv venv2.0
.\venv2.0\Scripts\activate
pip install -r requirements.txt
```

> `backend/.env` 和 `Agent/.env` 已包含所有密钥，`config.py` 自动读取，无需手动配置。

> **pyaudio 在 Windows 安装失败**时：
> ```powershell
> pip install pipwin && pipwin install pyaudio
> ```

---

## 2. 第一步：启动 MySQL 数据库（后台运行，无需终端）

MySQL 是本项目的**必需依赖**，Java 后端会将话数据写入 MySQL。

### 2.1 **[需手动] 安装并启动 MySQL**

#### Windows 安装 MySQL（使用 `.msi` 安装包，推荐）

1. 下载 [MySQL 8.0+ 社区版](https://dev.mysql.com/downloads/mysql/)
2. 选择 `Windows (x86, 64-bit), MSI Installer`
3. 安装过程中选择：
   - **Setup Type**: Custom（自定义）
   - **MySQL Server**: 勾选安装（版本 8.x）
   - **MySQL Workbench**: 可选（图形化管理工具）
4. 安装向导中设置 **root 用户密码**（记住它，下一步要用）
5. 安装完成后，确保 **MySQL Server** 服务已启动

#### macOS 安装 MySQL

```bash
brew install mysql
brew services start mysql
```

#### Linux (Ubuntu/Debian) 安装 MySQL

```bash
sudo apt update
sudo apt install mysql-server
sudo systemctl start mysql
sudo systemctl enable mysql
```

### 2.2 **[需手动] 创建数据库**

MySQL 启动后，用以下任一方式连接并创建数据库：

**方式 A：命令行（推荐）**

```bash
mysql -u root -p
```

输入安装时设置的 root 密码，连接成功后执行：

```sql
CREATE DATABASE IF NOT EXISTS wanqing_ai
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
```

按 `Ctrl+C` 退出。

**方式 B：DataGrip（图形界面，已安装 MySQL）**

1. 打开 DataGrip，左侧边栏点击 **Database**（数据库面板）
2. 点击 **+** → **Data Source** → **MySQL**
3. 填写连接信息：

   | 字段 | 值 |
   |------|-----|
   | Host | `localhost` |
   | Port | `3306` |
   | User | `root` |
   | Password | 你安装 MySQL 时设置的 root 密码 |
   | Database | （留空，先不填） |

4. 点击 **Test Connection**，确认显示绿色 "Successful"
5. 点击 **OK** 保存连接
6. 在左侧数据库面板中，右键你的 MySQL 连接 → **New** → **Query Console**
7. 在编辑器中输入并执行：

```sql
CREATE DATABASE IF NOT EXISTS wanqing_ai
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
```

8. 按 **Ctrl+Enter** 执行，右键连接 → **Refresh**，即可看到新建的 `wanqing_ai` 数据库

### 2.3 **[需手动] 配置数据库连接密码**

> **注意**：当前 `backend/src/main/resources/application.yml` 中的 MySQL 密码是硬编码的：。
>
> 如果你设置的 root 密码不同，需要修改此文件。

```yaml
# 文件位置：backend/src/main/resources/application.yml
spring:
  datasource:
    password: xxxxxxxxxx  # ← 改成你设置的密码
```

---

## 3. 第二步：启动 Redis（可选，后台运行）

Redis 用于 Agent 的实时感知数据存储，**不启动也不影响基本功能**（Agent 会降级为纯规则引擎）。

#### Windows（WSL，推荐）

```powershell
wsl
sudo apt update
sudo apt install redis-server
sudo redis-server --daemonize yes
```

#### macOS / Linux

```bash
brew install redis && brew services start redis
# 或
sudo apt install redis-server && sudo systemctl start redis
```

#### 验证 Redis

```bash
redis-cli ping
# 返回 PONG 即成功
```

> **在哪里执行？** — MySQL 和 Redis 都是后台常驻服务，在终端里启动后关掉终端窗口即可，会话结束后自动转为系统服务。

---

## 4. 终端 3：启动 Python 感知服务

> **端口：8000** — 负责摄像头+麦克风感知数据采集，WebSocket 实时推送视频流到前端

### 4.1 创建虚拟环境并安装依赖

```powershell
cd perception
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

> `backend/.env` 已包含所有 API 密钥，`config.py` 会自动读取，无需手动配置。

### 4.2 启动

```powershell
python main.py
```

**预期输出：**

```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

> 摄像头/麦克风不可用时，打印警告但**不会崩溃**，会自动降级运行。

### 4.3 验证

新开一个 PowerShell 窗口：

```powershell
curl http://localhost:8000/
# 应返回 {"status": "online", "service": "wanqing-perception", "port": 8000}
```

---

## 5. 终端 4：启动 Python Agent

> **端口：8001** — AI 大脑，负责情感推理和对话生成（LangGraph 决策引擎 + DeepSeek + RAG）

### 5.1 创建虚拟环境并安装依赖

```powershell
cd Agent
python -m venv venv2.0
.\venv2.0\Scripts\activate
pip install -r requirements.txt
```

> **首次安装时**，PyTorch（约 2GB）和 sentence-transformers 模型（约 1.5GB）会从网络下载，请确保网络畅通。

### 5.2 配置 API Key

编辑 `Agent/.env`（已存在，补充缺失的密钥即可）：

```env
# 必须填：DeepSeek（AI 推理大脑）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# 必须填：阿里云 DashScope（Qwen-VL 视觉分析 + TTS 语音合成）
QWEN_API_KEY=sk-your-qwen-api-key-here

# 可选：Notion 情绪日记同步
NOTION_API_KEY=ntn_your-notion-token
NOTION_DATABASE_ID=your-database-id
```

### 5.3 启动

```powershell
python main.py
```

**预期输出（首次启动约需 20~40 秒预热）：**

```
INFO:     Uvicorn running on http://0.0.0.0:8001
婉情AI 智能体就绪，监听 /internal/v1/agent/invoke
```

### 5.4 验证

新开一个 PowerShell 窗口：

```powershell
curl http://localhost:8001/health
# 应返回 {"status": "online", "service": "wanqing-agent"}
```

---

## 6. 终端 2：启动 Java Spring Boot 后端

> **端口：8080** — 业务网关，负责会话管理、API 路由，串联感知服务、Agent 和前端

### 6.1 确认 Java 版本

```powershell
java -version
# 确认包含 "21"（如 openjdk 21.0.x）
```

### 6.2 编译并启动

```powershell
cd backend
mvn spring-boot:run
```

> **首次运行**时，Maven 会下载所有依赖（约需 3~5 分钟），请耐心等待。

**预期输出：**

```
Started WanqingAiApplication in X.XXX seconds
```

### 6.3 验证

浏览器访问：`http://localhost:8080/swagger-ui.html`

看到 Swagger API 文档页面即启动成功。

---

## 7. 终端 1：启动 Vue 前端

> **端口：5173** — 可视化界面，展示婉晴立绘、情感雷达图和聊天窗口

### 7.1 安装依赖

```powershell
cd frontend
npm install
```

> `node_modules/` 已存在时跳过。

### 7.2 启动

```powershell
npm run dev
```

**预期输出：**

```
VITE ready in XXX ms
➜  Local:   http://localhost:5173/
➜  Network: http://192.168.x.x:5173/
```

### 7.3 访问

浏览器打开：`http://localhost:5173/`

> 首次打开时，浏览器会请求摄像头和麦克风权限，点击"允许"。

---

## 8. 一键启动脚本

四个服务建议使用独立终端启动，方便分别观察日志和调试。如果想一键启动，可以用脚本。

### 8.1 PowerShell（推荐）

项目根目录下：

```powershell
.\start_all.ps1
```

脚本会自动分步启动（不是并行，避免端口冲突）：

1. **终端 3**：Python 感知服务（8000）
2. **终端 4**：Python Agent（8001）
3. **终端 2**：Java Spring Boot（8080）
4. **终端 1**：Vue 前端（5173）

> 各服务之间有健康检查和等待，超时或失败不会阻塞其他服务。

### 8.2 Windows CMD（备选）

```cmd
cd backend
.\start_all.bat
```

### 8.3 Linux / macOS

```bash
chmod +x start_all.sh
./start_all.sh
```

---

## 9. 验证运行状态

启动完成后，四个终端应该各自显示就绪信息，检查端口确认全部 LISTENING：

```powershell
netstat -ano | findstr ":5173 :8000 :8001 :8080"
```

| 终端 | 端口 | 服务 | 预期输出关键词 |
|------|------|------|--------------|
| 终端 1 | 5173 | Vue 前端 | `Local: http://localhost:5173/` |
| 终端 2 | 8080 | Java Spring Boot | `Started WanqingAiApplication` |
| 终端 3 | 8000 | Python 感知服务 | `Uvicorn running on http://0.0.0.0:8000` |
| 终端 4 | 8001 | Python Agent | `婉情AI 智能体就绪` |

### 快速 API 健康检查

```powershell
curl http://localhost:8000/     # Python 感知服务
curl http://localhost:8001/health  # Python Agent
```

### 前端功能验证

浏览器打开 `http://localhost:5173/`：

1. 左上角显示婉晴立绘（动画呼吸效果）
2. 右上角显示 OCC 八维情感雷达图
3. 下方显示聊天输入框
4. F12 Console 无红色错误

---

## 10. 常见问题

### Q1：运行 `python main.py` 报 "ModuleNotFoundError"

**原因**：未激活 Python 虚拟环境或未安装依赖。

**解决方法**：

```bash
# 确认 Python 能找到 pip
python -m pip --version

# 安装依赖
cd backend
python -m pip install -r requirements.txt
```

### Q2：摄像头黑屏 / 麦克风无声音

1. 检查摄像头是否被其他程序占用（如微信、会议软件）
2. 关闭所有占用摄像头的应用后，重启感知服务
3. 浏览器隐私设置中允许 `localhost` 访问摄像头和麦克风
4. 感知服务打印警告但不崩溃——这是正常现象，说明摄像头暂不可用

### Q3：Agent 调用失败，婉晴不回复

1. 检查 `Agent/.env` 中的 `DEEPSEEK_API_KEY` 是否正确配置
2. 检查 `DEEPSEEK_API_KEY` 余额是否充足
3. 检查 Agent 是否启动成功（端口 8001 是否 LISTENING）
4. 如果 Agent 不可用，系统会自动降级为**规则引擎**（仍可回复基础对话）

### Q4：Java 启动报 MySQL 连接失败

1. 确认 MySQL 服务已启动（检查服务列表或 `netstat -ano | findstr :3306`）
2. 确认数据库 `wanqing_ai` 已创建
3. 确认 `application.yml` 中的用户名和密码正确

### Q5：Redis 连接失败

1. Redis 是**可选依赖**，不启动不影响基本功能（Agent 会降级）
2. 如果想使用 Redis，确保 `redis-server` 已启动
3. 确认 `redis-cli ping` 返回 `PONG`

### Q6：端口被占用

```powershell
# 查看哪个进程占用了端口（如 8080）
netstat -ano | findstr ":8080"

# 结束该进程（将 PID 替换为实际值）
taskkill /PID <PID> /F
```

### Q7：修改代码后不生效

| 服务 | 终端 | 重载方式 |
|------|------|---------|
| 终端 1 前端 | Vue | 保存文件后 Vite 自动热重载 |
| 终端 3 感知服务 | Python | 保存文件后 Uvicorn 自动重载 |
| 终端 4 Agent | Python | 保存文件后 Uvicorn 自动重载 |
| 终端 2 Java | Java | 需要重新 `mvn spring-boot:run` |

### Q8：前端长时间显示"婉晴感知系统正在启动中"

1. 确认**终端 2（Java，8080）**已启动成功，有 `Started WanqingAiApplication` 字样
2. F12 Console 查看具体网络错误
3. 检查 `mvn spring-boot:run` 是否有异常退出

---

## 服务健康检查命令汇总

```powershell
# 检查所有端口
netstat -ano | findstr ":5173 :8000 :8001 :8080 :3306 :6379"

# 测试各 API 是否响应
curl http://localhost:8000/                        # 感知服务
curl http://localhost:8001/health                 # Agent
# Java Swagger（浏览器直接打开）
http://localhost:8080/swagger-ui.html

# Redis
redis-cli ping
```

---

*本文档由 AI 根据项目配置自动生成，如有出入请以实际配置文件为准。*
*最后更新：2026-03-23*
