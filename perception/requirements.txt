# ==============================================================================
# Wanqing Backend - Python 依赖清单 ！！！！如果你出现utf-8的问题，无法pip install -r requirements.txt 
#请你把梯子关了，然后把这个文件的所有中文全部删除！！！
# 更新时间: 2026-03-21
# Python 版本要求: >= 3.10
# 重要更新 (2026-03-21)：新增 python-dotenv，用于从 backend/.env 加载 API 密钥
# ==============================================================================

# ------------------------------------------------------------------------------
# [感知模块] MediaPipe 视觉特征提取
# ------------------------------------------------------------------------------
mediapipe==0.10.21

# ------------------------------------------------------------------------------
# [感知模块] openSMILE 音频特征提取
# ------------------------------------------------------------------------------
opensmile==2.5.0

# ------------------------------------------------------------------------------
# [感知模块] HuggingFace 情绪/AU检测模型
# ------------------------------------------------------------------------------
transformers==4.50.3
torch==2.6.0                   # CPU版本，如有 GPU 请替换为对应 CUDA 版本

# ------------------------------------------------------------------------------
# [通信] FastAPI / WebSocket
# ------------------------------------------------------------------------------
fastapi==0.115.0
uvicorn==0.32.0
websockets==13.1
httpx>=0.28.0

# ------------------------------------------------------------------------------
# [音频] PyAudio 录制
# ------------------------------------------------------------------------------
pyaudio==0.2.14
pydub==0.25.1

# ------------------------------------------------------------------------------
# [图像处理]
# ------------------------------------------------------------------------------
opencv-python-headless==4.10.0.84   # cv2 (OpenCV)，headless 版兼容服务器/WSL 环境
Pillow>=10.0.0
numpy>=1.24.0

# ------------------------------------------------------------------------------
# [通义] 阿里云DashScope (Qwen/TTS/ASR)
# ------------------------------------------------------------------------------
dashscope>=1.20.0

# ------------------------------------------------------------------------------
# [OSS] 阿里云对象存储
# ------------------------------------------------------------------------------
oss2>=2.19.1

# ------------------------------------------------------------------------------
# [开发工具]
# ------------------------------------------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.25.0

# ------------------------------------------------------------------------------
# [缓存] Redis（感知微服务写入 Redis，Python Agent 读取）
# ------------------------------------------------------------------------------
redis>=5.0.0

# ------------------------------------------------------------------------------
# [配置] 环境变量加载（从 .env 文件读取 API 密钥等敏感配置）
# ------------------------------------------------------------------------------
python-dotenv>=1.0.0
openai>=1.0.0