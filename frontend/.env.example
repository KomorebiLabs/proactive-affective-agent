# 前端环境变量模板（复制为 .env.local 后按需修改；Vite 只暴露 VITE_ 前缀变量）
# Java 后端（会话初始化 / SSE 聊天 / 干预反馈）
VITE_API_BASE=http://localhost:8080
# 感知服务 WebSocket（视频帧 / 语音上传 / TTS 下发）
VITE_PERCEPTION_WS_URL=ws://localhost:8000/ws
